{
  "name": "exit-then",
  "description": "Execute promises before exiting a process",
  "version": "1.4.0",
  "author": "Jonathan Ong <me@jongleberry.com> (http://jongleberry.com)",
  "license": "MIT",
  "repository": "thenables/exit-then",
  "dependencies": {
    "native-or-bluebird": "1",
    "signal-exit": "^2.1.2",
    "timeout-then": "^1.0.0"
  },
  "devDependencies": {
    "bluebird": "3.1.1",
    "istanbul": "0",
    "mocha": "2"
  },
  "scripts": {
    "test": "mocha --reporter spec",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter dot"
  },
  "keywords": [
    "exit",
    "sigint",
    "sigterm",
    "cleanup",
    "process"
  ],
  "files": [
    "index.js"
  ]
}
